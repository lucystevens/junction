package uk.co.lucystevens.db.models


// TODO define database tables like below
/*object Flags : Table<Flag>(tableName = "flags", schema = "flaggle") {
    val id = int("flag_id").primaryKey().bindTo { it.id }
    val countryName = varchar("country_name").bindTo { it.countryName }
    val vectorFile = varchar("vector_file").bindTo { it.vectorFile }
    val iconCode = varchar("icon_code").bindTo { it.iconCode }
}

val Database.flags get() = this.sequenceOf(Flags)
*/
